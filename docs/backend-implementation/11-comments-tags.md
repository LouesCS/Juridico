# 11 — Comments e Tags *(não implementado nesta rodada)*

Schema pronto (`Comentario`, `ComentarioMencao`, `Tag` + tabelas
associativas). Contrato pronto (`docs/api/12-comments.md`,
`docs/api/09-legal-cases.md §9.6`). Ambos são CRUD relativamente simples —
bons candidatos a implementar logo após `LegalCases`/`Documents` existirem
(são os dois módulos que `Comments`/`Tags` referenciam).

---

**Anterior:** [10-documents-folders.md](10-documents-folders.md) · **Próximo:** [12-notifications.md](12-notifications.md)
