# 16 — Search

## Não implementado

Nenhum código escrito. Backend: módulo Search **não implementado** —
`GET /v1/search` não existe (as colunas `tsvector`/trigram já existem no
banco via migration manual, mas nenhum endpoint as consome ainda,
reafirma `docs/backend-implementation/19-decisions.md §19.11`). Command
Palette (⌘K) descrito em `docs/frontend/21-search.md` continua apenas
arquitetura.

---

**Anterior:** [15-notifications-sse.md](15-notifications-sse.md) · **Próximo:** [17-ai.md](17-ai.md)
