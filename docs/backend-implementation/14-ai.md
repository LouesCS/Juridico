# 14 — AI *(não implementado nesta rodada)*

Schema pronto (`ResumoIA`, `FonteIA`). Contrato pronto, incluindo o formato
de streaming SSE (`docs/api/14-ai.md §14.3`). `AIProviderPort` (pedido
explícito do Prompt 5B §28) **não foi criado**.

**Primeiro passo da próxima rodada:** `AIProviderPort` + adapter fake
(determinístico, para teste) antes de qualquer integração real — mesmo
padrão de `MailPort`. A regra "toda saída de IA registra modelo,
promptVersion, tokens e custo" já está inteiramente modelada no schema
(`ResumoIA`), só falta a orquestração.

---

**Anterior:** [13-search.md](13-search.md) · **Próximo:** [15-audit.md](15-audit.md)
